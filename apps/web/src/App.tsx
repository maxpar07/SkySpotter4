// apps/web/src/App.tsx
import { HomePage } from "./features/home/HomePage";

// Routing for Nearby / Detail / Logbook / Stats / Settings is added once
// those features are built, per the staged plan.
export function App() {
  return <HomePage />;
}
